import type { GameRules } from '../types/game';

export const gameRules: GameRules = {
  totalLevels: 10,
  secondsPerLevel: 60,
  passBonusScore: 10,
  quizCostScore: 5,
  quizCorrectTimeBonus: 10,
  leaderboardLimit: 10,
};
